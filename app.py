


def print_app():
    """ print_app is the name of our function and
    python knows this because
    we said we're going to declare it (def at the beginning)
    and the open close parenthesis at the end"""
    name = (__name__)
    """ dunder 'name' dunder is a specific built in pythonic method
    it is called a dunder method"""
    return name #'return' will give information back to whoever calls this function

"""this function will never run unless the 'print_app' is called"""


